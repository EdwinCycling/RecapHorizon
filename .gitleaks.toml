title = "RecapHorizon Gitleaks Configuration"

[allowlist]
description = "Ignore build artifacts and Firebase client keys in generated assets"
paths = [
  "assets/",
  "dist/",
  "node_modules/"
]
regexes = [
  """AIza[0-9A-Za-z\-_]{35}""",
  """apiKey\s*:\s*[\"\']AIza[0-9A-Za-z\-_]{35}[\"\']"""
]